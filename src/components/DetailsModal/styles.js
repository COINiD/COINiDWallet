import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  dialog: {
    borderRadius: 10,
    backgroundColor: '#FFF',
    width: '100%',
    maxHeight: '100%',
    overflow: 'hidden',
    alignSelf: 'center',
  },
});
