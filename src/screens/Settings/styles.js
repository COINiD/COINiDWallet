import { StyleSheet } from 'react-native';
import parentStyles from '../modalStyles';
import styleMerge from '../../utils/styleMerge';

export default (theme) => styleMerge(parentStyles(theme), StyleSheet.create({
}));